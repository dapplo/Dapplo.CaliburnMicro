<UserControl x:Class="Dapplo.CaliburnMicro.Demo.UseCases.Wizard.Views.WizardExampleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Dapplo.CaliburnMicro.Demo.Views"
			 xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
	<UserControl.Resources>
		<BooleanToVisibilityConverter x:Key="BooleanToVisibility" />
	</UserControl.Resources>
	<StackPanel>
		<TabControl x:Name="Items">
			<TabControl.ItemContainerStyle>
				<Style TargetType="TabItem" BasedOn="{StaticResource {x:Type TabItem}}">
					<Setter Property="IsEnabled" Value="{Binding IsEnabled}"/>
					<Setter Property="Visibility" Value="{Binding IsVisible,  Converter={StaticResource BooleanToVisibility} }"/>
				</Style>
			</TabControl.ItemContainerStyle>
		</TabControl>
		<UniformGrid>
			<Button x:Name="Previous" Content="{Binding WizardTranslations.Previous}"/>
			<Button x:Name="Next" Content="{Binding WizardTranslations.Next}"/>
			<Button x:Name="Finish" Content="{Binding WizardTranslations.Finish}"/>
			<Button x:Name="Cancel" Content="{Binding WizardTranslations.Cancel}"/>
		</UniformGrid>
	</StackPanel>
</UserControl>
